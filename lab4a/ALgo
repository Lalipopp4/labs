Пусть n - уровней в деревe, k - длина нужной ветки.

Чтение дерева: 
	max - O(2 ** n)

Вставка: 
	max - O(n)
	av - O(k)

Поиск: 
	max - O(n)
	av - O(k)

Удаление:
	max - O(n)
	av - O(k)

